# 测试数据配置文件（初始版本）
# 说明：1. 固定参数直接填写具体值；2. 需随机参数用 {random} 占位；3. 下拉框参数可配置可选值，便于后期扩展
# 业务流程：用户新增（跨3个页面：基本信息页→部门角色页→确认提交页）

# 页面1：用户基本信息页（输入框为主）
login_register_page:
  LoginName: "admin"
  LoginPassword: "admin123"

